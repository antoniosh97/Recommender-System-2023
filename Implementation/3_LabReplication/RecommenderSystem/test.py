import random
import numpy as np

sorted_data = np.ndarray([])
data = np.ndarray([])
sorted_data = np.random.randint(low=1, high=101, size=1)
print(sorted_data)
data = sorted_data[-1]#[:-1]
print(data)
